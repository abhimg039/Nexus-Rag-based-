# AI Document Intelligence & RAG Platform — backend dependencies
# Versions pinned to the set that is known to work together.
# Install with:  pip install -r requirements.txt

# --- Web framework ---
fastapi==0.128.8
uvicorn[standard]==0.39.0
python-multipart==0.0.20        # REQUIRED for file uploads (UploadFile)

# --- Database ---
SQLAlchemy==2.0.52
psycopg[binary]==3.2.13
pgvector==0.4.2

# --- Config / validation ---
pydantic==2.13.4
pydantic-settings==2.11.0
python-dotenv==1.2.1

# --- Document processing ---
pymupdf==1.26.5                 # imported as `fitz`

# --- Embeddings / ML ---
sentence-transformers==5.1.2
torch==2.8.0
numpy==2.0.2

# --- LLM transport (Ollama HTTP API) ---
requests==2.32.5
